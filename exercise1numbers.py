per_hour= 0.51
#float(input ("enter your per hour billing cost in pounds:"))
per_day = 24*per_hour
per_month= per_day*30
per_year=per_day*365 
twenty_servers_per_month= per_day*30*24
twenty_servers_per_day= per_day*24
print ("cost per day={}".format (per_day))
print ("cost per month={}".format (per_month))
print ("cost for twenty servers per month={} ".format (twenty_servers_per_month))
print ("cost for twenty servers per day={}".format(twenty_servers_per_day))



print("number of days it takes to operate a server by using 918:",918/per_hour/24)




































































































































































































































































































































































































































































































